extends default

block content
  .home-search
    img(src="/images/bk.png" width="180" height="180")
    form(action='/search' method='GET')
      input.home-input(autofocus='' placeholder='Search News' type='search' name='q')
